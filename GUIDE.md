# 🧽 스폰지 크루 인터뷰 스킬 빌딩 가이드

**Claude Code로 내 인터뷰 스킬 직접 만들기 · 빈 폴더에서 GitHub까지**

---

## 들어가며 — 왜 우리가 직접 만드는가

스폰지 인터뷰 스킬은 셀피쉬클럽이 만들어 배포한 스킬입니다. 그건 시작점이에요. 7주를 굴리다 보면 "내 업무에는 이런 질문이 더 필요한데" 같은 지점이 반드시 옵니다.

이 가이드는 그 지점에서 **여러분이 직접 본인만의 인터뷰 스킬을 만들 수 있도록** 안내합니다. 정답은 없습니다. 마케터의 인터뷰 스킬과 1인 대표의 인터뷰 스킬은 다르게 생겨야 정상이에요.

7주 끝나는 날, 45개의 인터뷰 스킬이 GitHub에 흩어져 있는 풍경 — 그게 이 작업의 그림입니다.

---

## 0. 시작 전에 — 모두 같은 출발선 만들기

### 0-1. 스폰지클럽이 무엇이고, 이 스킬이 그 안에서 어디에 있는가

스폰지클럽은 셀피쉬클럽이 1년 동안 내부에서 양성한 AI 크루 프로그램의 외부 첫 출시판입니다. 7주 동안 두 트랙을 굴려요.

| 트랙 | 무엇 |
|---|---|
| **트랙 A — 나만의 AI OS** (W2부터) | 매일 내가 켜서 내 업무·삶을 같이 굴리는 AI 작업 환경 |
| **트랙 B — 유저가 쓸 프로덕트** (W4부터) | 진짜 유저가 만져볼 수 있는 결과물 1개 |

둘 다 만드는 게 권장이지만, 한쪽을 깊이 파는 것도 OK입니다.

**이 스킬이 하는 일**: 트랙 A든 B든, 그걸 만들기 전에 **나에게 인터뷰를 걸어서 방향을 날카롭게 잡아주는 도구**입니다. 셀피쉬클럽이 W1에 배포한 스폰지 인터뷰 스킬이 모든 크루에게 같은 질문을 던졌다면, 여러분이 만드는 스킬은 **여러분의 일에 꽂히는 질문**을 던집니다.

### 0-2. 스킬이라는 게 정확히 뭔가

스킬(Skill)은 Claude가 특정 작업을 수행할 때 따르는 **재사용 가능한 규칙 묶음**입니다. 폴더 하나 안에 다음이 들어 있어요:

```
my-interview-skill/
├── SKILL.md          ← 스킬의 본체. 언제 트리거되고 어떻게 동작하는지
└── (선택) 보조 파일들  ← 질문 템플릿, 출력 포맷 예시 등
```

핵심은 `SKILL.md` 한 파일입니다. Claude가 이 파일을 읽고 "아, 사용자가 이런 상황이면 이 절차를 따라야 하는구나"를 이해해요.

### 0-3. 작업 전체 흐름 한눈에

이 가이드는 **7단계**로 진행됩니다. 처음 한 바퀴 도는 데 약 **2~3시간** 잡으시면 돼요.

| Step | 무엇 | 도구 | 예상 시간 |
|---|---|---|---|
| 1 | 환경 준비 (Claude Code, GitHub 계정) | 브라우저 + 터미널 | 30분 |
| 2 | 내 스킬 설계 워크시트 작성 | 종이 or 노션 | 30분 |
| 3 | 빈 폴더 만들고 Claude Code 켜기 | 터미널 | 5분 |
| 4 | Claude Code와 자연어로 SKILL.md 만들기 | Claude Code | 40~60분 |
| 5 | 만든 스킬 실제로 써보고 다듬기 | Claude Code/Desktop | 30분 |
| 6 | GitHub repo 만들고 업로드 | 브라우저 + 터미널 | 30분 |
| 7 | #냅다-공유에 던지기 | 슬랙 | 5분 |

### 0-4. 시작 전에 챙길 것

- [ ] 노트북
- [ ] **Claude Code Max 플랜** ($100/월 권장 — 1기 표준 도구)
- [ ] **Claude Desktop 앱** (스킬 테스트용, 무료)
- [ ] **GitHub 계정** (없으면 Step 6에서 같이 만듭니다, 무료)
- [ ] **터미널 앱** — Mac은 기본 "터미널", Windows는 "PowerShell" 또는 "Windows Terminal"
- [ ] 30분~3시간 통째로 비울 수 있는 시간

---

## Step 1 · 환경 준비

> **목표**: Claude Code가 내 컴퓨터에서 켜지고, GitHub 계정이 살아있는 상태

### 1-1. Claude Code 설치 확인

이미 W1 워크숍 VOD를 보고 깔았다면 다음으로. 아직이면:

1. https://claude.com/code 에서 안내대로 설치
2. 터미널 열고 `claude --version` 입력 → 버전 번호 뜨면 OK
3. `claude` 입력 → 로그인 안내 따라가기

**막히면**: #질문-답변 채널에 "Claude Code 설치"로 검색. 같은 자리에서 막힌 사람이 이미 있을 거예요.

### 1-2. GitHub 계정 만들기 (이미 있으면 스킵)

1. https://github.com 접속 → "Sign up"
2. 이메일·비밀번호·유저네임 입력
   - 유저네임은 짧고 영문으로 (이게 나중에 URL이 됩니다 — 예: `github.com/내유저네임`)
3. 이메일 인증
4. 무료 플랜 선택

### 1-3. Claude Desktop 깔기 (스킬 테스트용)

https://claude.com/download 에서 Claude Desktop 다운로드 → 설치.

> 💡 Claude Desktop과 Claude Code는 다른 프로그램입니다. Code는 터미널에서 코드를 짜고, Desktop은 일반 채팅 + 스킬 실행 환경. 둘 다 있으면 좋아요.

---

## Step 2 · 내 스킬 설계 워크시트

> **목표**: 코드 한 줄 짜기 전에, **내가 만들 스킬이 무엇이고 누구에게 어떤 질문을 던질지** 종이에 먼저 그리기

이 단계를 건너뛰면 Step 4에서 Claude Code에게 막연한 지시를 하게 됩니다. 30분만 투자하면 Step 4가 두 배 빨라져요.

> 💡 이 키트의 [`worksheet.md`](./worksheet.md)를 복사해서 본인 답으로 채우면 가장 빠릅니다.

### 워크시트

아래 6개 칸을 본인 답으로 채우세요. 종이, 노션, 메모장 어디든 OK.

---

**① 이 스킬이 인터뷰하는 대상은 누구인가**

내가 7주 뒤 이 스킬을 누구에게 쥐여 줄 것인가. 나 자신? 우리 팀원? 우리 클라이언트? 우리 서비스의 유저?

> 예시:
> - "마케팅 대행사에 의뢰하는 중소기업 대표 — 우리 서비스 브리프를 받기 전에 그분들의 비즈니스 맥락을 먼저 인터뷰해서 정리한다"
> - "나 자신 — 매주 일요일 저녁 한 주를 회고할 때 나에게 인터뷰를 건다"
> - "유튜브 채널에 들어오는 신규 시청자 — 어떤 콘텐츠가 필요한지 1:1로 인터뷰"

내 답:
```
(여기에 적기)
```

---

**② 이 인터뷰가 끝나면 무엇이 손에 남아야 하는가 (출력물)**

1장짜리 브리프? 2~3개 페르소나? 다음 주에 만들 콘텐츠 5개의 제목? 정확한 산출물의 모양을 그려두세요.

> 예시:
> - "1페이지 클라이언트 브리프 — 비즈니스 맥락, 목표, 예산, 기대 결과물"
> - "이번 주 회고 카드 — 잘한 것 3, 막힌 것 1, 다음 주 1개 우선순위"
> - "유저 페르소나 1장 + 추천 콘텐츠 토픽 5개"

내 답:
```
(여기에 적기)
```

---

**③ 이 인터뷰는 어떤 단계로 흘러야 하는가 (오케스트레이션)**

스폰지 인터뷰 스킬이 워밍업 → 맥락 → 반복 지점 → 레버리지 → 검색 → 선언문 순으로 흐른 것처럼, 내 스킬도 단계가 있어야 합니다. 4~6단계 권장.

> 예시 (마케터의 클라이언트 브리핑 스킬):
> 1. 워밍업 — 클라이언트 회사 소개
> 2. 비즈니스 맥락 — 무엇을 팔고, 누구에게, 어떻게
> 3. 진짜 문제 발굴 — 지금 가장 풀고 싶은 1개
> 4. 기존 시도와 실패 — 이미 해본 것
> 5. 예산·일정·기대치
> 6. 정리 — 1페이지 브리프 출력

내 답:
```
1.
2.
3.
4.
5.
6.
```

---

**④ 어떤 질문이 핵심인가 (질문 풀)**

각 단계마다 던질 핵심 질문 1~2개씩. 막연하지 않게 구체적으로.

> 좋은 예: "최근 한 달간 가장 많은 시간을 쓴 업무 1개를 알려주세요. 그 업무를 한 줄로 묘사한다면?"
>
> 안 좋은 예: "당신의 업무에 대해 알려주세요" (너무 막연 → 답이 산으로 감)

내 답:
```
단계 1 질문:
단계 2 질문:
단계 3 질문:
...
```

---

**⑤ 인터뷰 대상이 추상적으로 빠질 때 어떻게 잡아둘 것인가 (하네스)**

사람들이 인터뷰에 답할 때 자주 추상적인 답을 합니다 — "고객 만족이 중요해요" 같은. 이때 "구체적으로 최근 1주일 안에 일어난 일 하나만 알려주세요"로 끌어오는 규칙이 하네스예요.

> 예시:
> - "추상적인 단어가 3개 이상 연속으로 나오면 → 최근 1주일 안의 구체적 사례 1개를 요청한다"
> - "답이 100자 미만이면 → '조금 더 풀어주실 수 있을까요?' 자동 추가"

내 답:
```
(2~3개의 하네스 규칙)
```

---

**⑥ 이 스킬은 언제 자동으로 트리거되어야 하는가**

Claude Desktop/Code에서 스킬은 사용자 메시지의 의도를 보고 자동 호출됩니다. 어떤 단어·상황에서 내 스킬이 발동되어야 하는가.

> 예시:
> - "사용자가 '클라이언트 브리프'·'신규 고객 미팅'·'프로젝트 시작'을 언급하면"
> - "사용자가 '한 주 회고'·'이번 주 정리'를 언급하면"

내 답:
```
(트리거 키워드 리스트)
```

---

✅ 6개 칸을 다 채웠으면 Step 3로. 비어있는 칸이 있으면 Step 4가 헷갈려져요. 일단 거친 답이라도 적어두세요.

---

## Step 3 · 빈 폴더 만들고 Claude Code 켜기

> **목표**: 작업할 빈 폴더 하나 만들고, 그 안에서 Claude Code를 띄우기

### 3-1. 작업 폴더 만들기

터미널을 엽니다.

**Mac**:
```bash
cd ~/Documents
mkdir my-interview-skill
cd my-interview-skill
```

**Windows (PowerShell)**:
```powershell
cd ~\Documents
mkdir my-interview-skill
cd my-interview-skill
```

> 💡 폴더 이름은 본인이 알아볼 수 있게. 영문·하이픈 권장 (`vivian-client-brief-skill`처럼). 한글은 가능하지만 GitHub에서 가끔 깨집니다.

### 3-2. Claude Code 띄우기

같은 터미널 창에서:
```bash
claude
```

Claude Code가 켜지면서 현재 폴더(`my-interview-skill`)를 작업 디렉토리로 인식합니다. 여기서부터는 자연어로 대화합니다.

---

## Step 4 · Claude Code와 자연어로 SKILL.md 만들기

> **목표**: 워크시트의 답을 Claude Code에게 넘겨서 SKILL.md 초안을 만들고, 대화로 다듬기

### 4-1. 첫 프롬프트 — 맥락 던지기

Claude Code에게 다음과 같이 통째로 던집니다. 워크시트 답을 그대로 붙여넣어요.

```
나는 스폰지클럽 1기 크루이고, 7주 동안 나만의 AI OS 또는 유저 프로덕트를
만드는 과정에서 사용할 인터뷰 스킬을 만들고 싶어.

이 스킬은 Claude Desktop/Code에서 자동 트리거되어, 인터뷰 대상에게
질문을 던지고 결과물을 출력하는 스킬이야.

내 스킬의 설계는 이래:

【인터뷰 대상】
(워크시트 ① 답 붙여넣기)

【최종 출력물】
(워크시트 ② 답 붙여넣기)

【인터뷰 단계 (오케스트레이션)】
(워크시트 ③ 답 붙여넣기)

【핵심 질문】
(워크시트 ④ 답 붙여넣기)

【하네스 규칙】
(워크시트 ⑤ 답 붙여넣기)

【트리거 조건】
(워크시트 ⑥ 답 붙여넣기)

이 내용을 바탕으로 현재 폴더에 SKILL.md를 만들어줘.
SKILL.md는 다음 구조를 따라야 해:

0. 최상단에 YAML frontmatter (name, description) — Claude가 트리거 판단할 때 읽음
1. 스킬 이름과 한 줄 설명
2. "When to use this skill" 섹션 — 트리거 조건
3. "How this skill works" 섹션 — 오케스트레이션 흐름
4. "Questions" 섹션 — 단계별 질문 풀
5. "Harness rules" 섹션 — 하네스 규칙
6. "Output format" 섹션 — 최종 출력 템플릿

작성하기 전에, 내가 적은 내용 중 모호하거나 보강이 필요한 지점이
있으면 먼저 1~3개 질문을 던져줘. 답한 다음에 SKILL.md를 만들어.
```

> 💡 이 키트의 [`templates/SKILL.md.template`](./templates/SKILL.md.template)을 참고로 같이 던져주면 더 좋아요. "이 템플릿 구조에 맞춰 채워줘"로 한 번에 끝납니다.

### 4-2. Claude Code의 질문에 답하기

Claude Code가 워크시트의 빈약한 부분을 짚어줄 거예요. 예를 들어:

> "③ 단계 4 '기존 시도와 실패' 질문이 비어있어. 어떤 각도로 물어볼 거야? 실패한 이유를 캐는 방향인지, 아니면 어떤 시도가 있었는지 매핑하는 방향인지?"

이런 질문에 답하면서 본인의 스킬이 더 또렷해집니다. 이 대화 자체가 W1에서 말한 "AI에게 인터뷰당하기"의 한 형태예요.

### 4-3. SKILL.md 초안 받기

질문이 끝나면 Claude Code가 `SKILL.md` 파일을 폴더에 만듭니다. 다른 터미널 창에서 확인:

```bash
ls
cat SKILL.md
```

또는 그냥 Claude Code에게 "방금 만든 SKILL.md 보여줘"라고 해도 됩니다.

### 4-4. 다듬기 — 다음 같은 질문으로 압박

초안이 나오면 한 번에 통과시키지 마세요. 다음 같은 질문으로 다듬어보세요:

- "단계 3의 질문이 너무 일반적이야. 마케터가 던질 법한 더 구체적인 표현으로 다시 써줘."
- "하네스 규칙이 막연해. '추상적이면 끊는다'를 어떻게 판단하는지 더 명확하게 적어줘."
- "출력 포맷에 예시를 하나 추가해줘. 가상의 클라이언트 한 명을 인터뷰한 결과물 샘플."

3~5번 정도 왕복하면 거친 v0.1이 나옵니다.

### 4-5. (선택) 보조 파일 만들기

질문 풀이 길거나 출력 템플릿이 복잡하면 별도 파일로 빼는 게 좋아요. 예:

```
my-interview-skill/
├── SKILL.md
├── questions.md       ← 질문 풀 따로
└── output_template.md ← 출력 템플릿 예시
```

Claude Code에게 "질문 풀이 너무 길어서 SKILL.md가 무거워. questions.md로 분리하고 SKILL.md에서 참조하게 바꿔줘"라고 지시하면 자동으로 처리해줍니다.

---

## Step 5 · 만든 스킬 실제로 써보고 다듬기

> **목표**: 내 스킬을 실제로 발동시켜보고, 어색한 지점 잡아내기

### 5-1. 스킬을 전역(global)에 등록하기

스킬은 어떤 프로젝트 폴더에서든 트리거되어야 진짜 쓸모가 생겨요. 클라이언트 브리핑이든, 주간 회고든, 콘텐츠 기획이든 — 인터뷰가 필요한 모든 자리에서.

**Claude Code에서 전역으로 쓰기**:

Claude Code는 두 위치에서 스킬을 찾습니다.
- **전역**: `~/.claude/skills/<스킬이름>/SKILL.md` ← 권장
- **프로젝트별**: `<프로젝트>/.claude/skills/<스킬이름>/SKILL.md`

만든 스킬을 어떤 프로젝트에서든 트리거하려면:

```bash
# 작업 폴더를 통째로 전역 skills 폴더로 복사
mkdir -p ~/.claude/skills
cp -r ~/Documents/my-interview-skill ~/.claude/skills/
```

또는 **심볼릭 링크**로 걸어두면 GitHub repo에서 수정한 게 그대로 반영됩니다 (추천):

```bash
mkdir -p ~/.claude/skills
ln -s ~/Documents/my-interview-skill ~/.claude/skills/my-interview-skill
```

이렇게 하면 어떤 폴더에서 `claude`를 켜도 본인의 인터뷰 스킬이 발동됩니다.

**Claude Desktop에서 쓰기**:

Claude Desktop의 스킬 등록 인터페이스는 시점에 따라 달라질 수 있어요. **가장 정확한 절차는 https://docs.claude.com 에서 "Skills"를 검색**해서 확인하세요. 1기 워크숍 VOD에서 다룬 절차도 같이 참고.

### 5-2. 트리거 발동 테스트

워크시트 ⑥에 적은 트리거 키워드가 들어간 메시지를 던져보세요.

> 예: "오늘 새로운 클라이언트 미팅이 잡혔어. 브리프를 만들어야 해."

스킬이 자동으로 발동되어 첫 질문을 던지면 성공.

**발동되지 않으면**: SKILL.md frontmatter의 `description`이 너무 모호하거나, 트리거 키워드가 약한 거예요. Claude Code로 돌아가 "트리거 설명을 더 강하게 다시 써줘"라고 요청.

### 5-3. 끝까지 한 번 굴려보기

내 스킬을 처음부터 끝까지 한 번 돌려봅니다. 본인이 직접 인터뷰 대상이 되어 답해보세요. 다음을 체크:

- [ ] 단계가 자연스럽게 흐르는가, 어디서 어색하게 끊기는가
- [ ] 질문이 너무 추상적이지 않은가
- [ ] 하네스가 작동하는가 (일부러 추상적으로 답해보고, 스킬이 잡아주는지)
- [ ] 최종 출력물이 실제로 쓸 만한가

문제 지점을 메모해두고, Claude Code로 다시 돌아가 수정.

### 5-4. v0.1 → v0.2 사이클

처음부터 완벽하게 만들 수 없습니다. 3~5바퀴 돌면서 다듬는다고 보세요. 매주 한 바퀴씩 굴려도 W7쯤이면 꽤 단단한 v1.0이 나와있을 거예요.

---

## Step 6 · GitHub에 업로드

> **목표**: 내 스킬을 GitHub에 올려서 다른 크루도 볼 수 있게, 그리고 내 변경 이력이 남게

### 6-1. GitHub에 빈 repo 만들기

1. https://github.com 접속, 로그인
2. 우측 상단 `+` 버튼 → `New repository`
3. 다음 입력:
   - **Repository name**: `my-interview-skill` (Step 3에서 만든 폴더 이름과 동일하게)
   - **Description**: 한 줄로 (예: "스폰지클럽 1기 - 클라이언트 브리프 인터뷰 스킬")
   - **Public** 선택 (다른 크루가 보려면 공개)
   - **README, .gitignore, license는 모두 체크 해제** (이미 우리 폴더에 파일이 있어서 충돌 방지)
4. `Create repository` 클릭

### 6-2. 만든 repo URL 복사

repo가 만들어지면 화면에 안내가 뜹니다. 그중 다음 같은 부분을 찾으세요:

```
…or push an existing repository from the command line

git remote add origin https://github.com/내유저네임/my-interview-skill.git
git branch -M main
git push -u origin main
```

이 세 줄을 그대로 복사. 자기 유저네임이 들어간 그 줄이 핵심이에요.

### 6-3. 터미널에서 업로드

Step 3에서 만든 폴더로 돌아가서:

```bash
cd ~/Documents/my-interview-skill   # 본인 경로로
```

처음 GitHub에 올리는 거면 다음 명령들을 차례로 입력합니다.

```bash
# (처음 한 번만) Git에 본인 정보 알려주기
git config --global user.name "본인 GitHub 유저네임"
git config --global user.email "본인 GitHub 가입 이메일"

# 이 폴더를 Git이 추적하기 시작
git init

# 모든 파일 스테이징
git add .

# 첫 커밋
git commit -m "first commit: interview skill v0.1"

# 메인 브랜치 이름 정리
git branch -M main

# 원격 저장소(GitHub) 연결 — 6-2에서 복사한 그 URL
git remote add origin https://github.com/내유저네임/my-interview-skill.git

# 업로드
git push -u origin main
```

마지막 명령에서 GitHub 로그인을 요구할 수 있습니다. 안내대로 진행.

> 💡 처음에는 비밀번호가 아니라 **Personal Access Token**을 요구할 수 있어요. github.com → Settings → Developer settings → Personal access tokens → Generate new token (classic) → repo 권한 체크 → 생성된 토큰을 비밀번호 자리에 붙여넣기.
>
> **더 쉬운 방법**: `gh` (GitHub CLI)를 설치하면 `gh auth login` 한 번으로 끝납니다. https://cli.github.com

### 6-4. 업로드 확인

브라우저에서 `https://github.com/내유저네임/my-interview-skill` 접속. SKILL.md가 보이면 성공.

> 🎉 **이 시점에서 여러분은 GitHub에 본인의 첫 스킬을 올린 사람이 됐습니다.**

### 6-5. 다음 변경부터는 3줄로

이제 SKILL.md를 수정할 때마다:

```bash
git add .
git commit -m "v0.2: 단계 3 질문 보강"
git push
```

이 3줄이 끝. Step 5에서 다듬을 때마다 이걸 굴리면 변경 이력이 GitHub에 차곡차곡 쌓입니다.

### 6-6. README.md 추가하기 (권장)

repo 첫 화면에 보이는 안내문이에요. Claude Code에게 한 줄로 부탁하면 알아서 만들어줍니다.

```
이 repo의 README.md를 만들어줘. 다음 내용 포함:
- 스킬 이름과 목적
- 누구를 위한 스킬인지
- 어떻게 설치/사용하는지 (전역 등록 방법 포함)
- 스폰지클럽 1기 크루의 작업물이라는 한 줄
```

만들어진 README.md를 다시 push:

```bash
git add README.md
git commit -m "add README"
git push
```

---

## Step 7 · #냅다-공유에 던지기

> **목표**: 이게 진짜 마지막 단계. 내가 만든 걸 조 채널에 풀어놓기

스폰지클럽의 가장 중요한 원칙이 이거예요:

> "만드는 것도 중요하나, 이기적 공유가 더 중요하다." — AAA 6주 베타의 결론

만든 채로 두면 1주치 학습. #냅다-공유에 던지면 8~10주치가 돌아와요.

### 던지는 메시지 템플릿

```
🧽 [내 이름] · 인터뷰 스킬 v0.1 풀었습니다

[한 줄 소개]
이 스킬은 _________________을 인터뷰해서
_________________을 만들어내는 스킬이에요.

[GitHub]
https://github.com/내유저네임/my-interview-skill

[지금 막힌 지점]
_________________
(없으면 비워도 OK)

[누구한테 도움될 것 같은지]
이런 분들 — _________________

자유롭게 이슈 남겨주시거나, fork해서 본인 버전으로 가져가셔도 됩니다.
```

### 다른 크루가 fork 했을 때

본인이 만든 스킬을 다른 크루가 자기 버전으로 가져가는 게 fork입니다. 그 사람의 GitHub repo가 내 repo의 자식처럼 생겨요. 5명이 fork하면 한 갈래에서 6개의 다른 인터뷰 스킬이 자라기 시작합니다.

이게 **이기적 공유의 가장 또렷한 형태** — 내가 처음에 던진 한 개가 6개로 돌아오는.

---

## 부록 A · Claude Code에게 자주 쓰면 좋은 프롬프트 모음

```
방금 만든 SKILL.md를 처음부터 끝까지 한 번 굴려보고,
인터뷰 대상이 답하기 어려워할 만한 질문이 있으면 짚어줘.
```

```
이 스킬에서 가장 약한 부분이 어디라고 생각해? 솔직하게.
```

```
가상의 인터뷰 대상 1명을 만들어서, 내 스킬로 끝까지
인터뷰를 진행한 결과물을 보여줘. 그러면서 어디가 어색한지 짚어줘.
```

```
SKILL.md가 너무 길어. 핵심을 잃지 않으면서 30% 줄여줘.
```

```
이 스킬을 다른 크루(예: 1인 대표)가 fork해서 쓰려고 할 때,
어떤 부분을 그 크루가 가장 먼저 본인 맥락에 맞게 바꿔야 할지
README에 한 섹션으로 안내해줘.
```

---

## 부록 B · 막혔을 때 어디로 가는가

| 막힌 지점 | 어디로 |
|---|---|
| Claude Code 설치·환경 | #질문-답변 채널, 검색어: "Claude Code" |
| Git/GitHub 명령어 에러 | #질문-답변 채널, 에러 메시지 그대로 붙여넣기 |
| 내 스킬 설계가 막연함 | 조 채널에 워크시트 ①~⑥ 답 던지기 — 부조장이 잡아줌 |
| 스킬 트리거가 안 발동 | https://docs.claude.com → "Skills" 문서 다시 확인 |
| "내 유즈케이스가 인터뷰 스킬에 안 어울리는 것 같음" | 조장에게 1:1 — 인터뷰가 아닌 다른 형태가 맞을 수도 있음 |

> 막힘은 자산입니다. 막혔던 지점과 어떻게 풀었는지 #빌딩로그에 남기면 다음 사람의 길이 됩니다.

---

## 마지막 한 마디

이 가이드를 따라 만든 스킬은 **여러분의 첫 번째 작품**이 될 거예요. 7주가 끝날 때쯤이면 v0.1이 v1.0이 되고, GitHub의 커밋 히스토리에 본인이 어떻게 굴렸는지가 통째로 남아있을 겁니다.

스폰지클럽이 W1에 배포한 인터뷰 스킬은 모두에게 같은 질문을 던졌어요. 여러분이 만드는 스킬은 **여러분의 일에서만 나올 수 있는 질문**을 던집니다. 그게 다른 사람에게는 절대 만들어지지 않는 자산이에요.

> 냅다 흡수하고 · 냅다 만들고 · 냅다 세상에 내놓는다

W7에서 봐요. 🧽
